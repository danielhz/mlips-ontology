# Python build dependencies for the dataset pipeline.
#
# Used by `make ontology` (the XHTML -> OWL/TTL build, via
# artifacts/scripts/merge_rdfa_into_owl.py) and `make term-appendices`
# (generate_term_appendix.py). The dataset itself has no Python runtime
# dependency; these are build-time only.
#
# The Makefile installs these automatically into a local, gitignored
# ./.venv the first time a Python step runs (see the `venv` target).
rdflib>=7.0,<8
pyRdfa3>=3.6
