# Python build dependencies for the dataset pipeline.
#
# Used by `make ontology` (the XHTML -> OWL/TTL build, via
# artifacts/scripts/merge_rdfa_into_owl.py) and `make term-appendices`
# (generate_term_appendix.py). The dataset itself has no Python runtime
# dependency; these are build-time only.
#
# The Makefile installs these automatically into a local, gitignored
# ./.venv the first time a Python step runs (see the `venv` target).
rdflib>=7.0,<8
pyRdfa3>=3.6,<4
html5lib>=1.1            # entity-aware XHTML DOM parse in extract_modules.py
pyshacl>=0.26,<0.32     # SHACL validation of the module shape (make shacl)
cffconvert>=2.0,<3       # CITATION.cff -> codemeta.json (make codemeta)

# Exact versions are frozen in requirements.lock (preferred by the
# Makefile venv target when present) so the published RDF build is
# reproducible; this file is the human-readable spec. Regenerate with:
#   .venv/bin/pip freeze > requirements.lock
