% Auto-generated from Graphviz layout — edit freely
% Regenerate initial layout: python dot_to_tikz.py
\begin{tikzpicture}[
    x=1cm, y=1cm,
    every node/.style={font=\small},
    class/.style={draw, rounded corners=2pt, fill=classfill,
        font=\small, inner sep=3pt, align=center},
    instance/.style={draw, fill=instancefill,
        font=\small, inner sep=3pt, align=center},
    literal/.style={font=\small\color{gray}, inner sep=2pt, align=center},
    modulenode/.style={font=\small, inner sep=2pt, align=center},
    extclass/.style={draw, dashed, rounded corners=2pt, fill=extfill,
        font=\small, inner sep=3pt, align=center},
    edge/.style={->, >=stealth, thick},
    crossedge/.style={->, >=stealth, thick, dashed},
    edgelabel/.style={font=\scriptsize, fill=white, inner sep=1pt},
]

% Color definitions (copy to preamble if not already defined):
% \definecolor{classfill}{HTML}{DDEEFF}
% \definecolor{instancefill}{HTML}{FFF4CC}
% \definecolor{extfill}{HTML}{E8E8E8}
% \definecolor{algofill}{HTML}{C6DBEF}
% \definecolor{trainfill}{HTML}{C7E9C0}
% \definecolor{benchfill}{HTML}{FDD0A2}

  % Nodes
  \node[instance] (mace) at (2.03,8.96) {mlips:mace-algorithm \\ (MLIPAlgorithm) \\ «MACE»};
  \node[instance] (hp_cutoff) at (10.50,4.20) {mlips:mace-cutoff-radius \\ (Hyperparameter)};
  \node[instance] (hp_layers) at (10.50,6.10) {mlips:mace-num-layers \\ (Hyperparameter)};
  \node[instance] (hp_lr) at (10.50,8.01) {mlips:mace-learning-rate \\ (Hyperparameter)};
  \node[instance] (impl) at (10.50,9.95) {mlips:mace-impl-v03 \\ (Implementation) \\ «MACE v0.3»};
  \node[instance] (lib) at (19.31,9.45) {mlips:lib-mace-python \\ (Library)};
  \node[instance] (sim_md) at (10.50,11.92) {mlips:sim-md \\ (SimulationType) \\ «Molecular dynamics»};
  \node[instance] (sim_opt) at (10.50,13.90) {mlips:sim-geometry-opt \\ (SimulationType) \\ «Geometry opt.»};
  \node[modulenode] (l_name) at (19.31,1.38) {"cutoff\_radius"};
  \node[modulenode] (l_default) at (19.31,3.28) {"5.0"\textasciicircum{}\textasciicircum{}xsd:double};
  \node[modulenode] (l_min) at (19.31,5.19) {"2.0"\textasciicircum{}\textasciicircum{}xsd:double};
  \node[modulenode] (l_max) at (19.31,7.09) {"10.0"\textasciicircum{}\textasciicircum{}xsd:double};
  \node[modulenode] (l_version) at (19.31,11.36) {"0.3.0"};

  % Edges
  \draw[edge] (mace) -- node[edgelabel, pos=0.5] {hasHyperparameter} (hp_cutoff);
  \draw[edge] (mace) -- node[edgelabel, pos=0.5] {hasHyperparameter} (hp_layers);
  \draw[edge] (mace) -- node[edgelabel, pos=0.5] {hasHyperparameter} (hp_lr);
  \draw[edge] (mace) -- node[edgelabel, pos=0.5] {hasImplementation} (impl);
  \draw[edge] (mace) -- node[edgelabel, pos=0.5] {supportsSimulation} (sim_md);
  \draw[edge] (mace) -- node[edgelabel, pos=0.5] {supportsSimulation} (sim_opt);
  \draw[edge] (hp_cutoff) -- node[edgelabel, pos=0.5] {hyperparameterName} (l_name);
  \draw[edge] (hp_cutoff) -- node[edgelabel, pos=0.5] {defaultValue} (l_default);
  \draw[edge] (hp_cutoff) -- node[edgelabel, pos=0.5] {minValue} (l_min);
  \draw[edge] (hp_cutoff) -- node[edgelabel, pos=0.5] {maxValue} (l_max);
  \draw[edge] (impl) -- node[edgelabel, pos=0.5] {implementedIn} (lib);
  \draw[edge] (impl) -- node[edgelabel, pos=0.5] {version} (l_version);

\end{tikzpicture}