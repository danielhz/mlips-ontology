% Manually-laid-out alignment figure.
%
% Visual conventions:
%   * Line width   = number of axioms collapsed onto the arc
%       1 axiom  = thin (0.5pt)
%       2 axioms = medium (1.0pt)
%       3 axioms = thick (1.5pt)
%   * Solid (filled) circles = MLIPs ontology modules
%   * Dashed circles         = external ontologies
\begin{tikzpicture}[
    x=1cm, y=1cm,
    every node/.style={font=\small},
    nodemod/.style={draw, circle, fill=classfill,
        minimum size=1.5cm, inner sep=0pt, align=center,
        font=\fontsize{8}{9.6}\selectfont},
    nodeext/.style={draw, dashed, circle, fill=extfill,
        minimum size=1.5cm, inner sep=0pt, align=center,
        font=\fontsize{8}{9.6}\selectfont},
    arc/.style={-},
    edgelabel/.style={font=\scriptsize, fill=white, inner sep=1pt},
]

% Color definitions (copy to preamble if not already defined):
% \definecolor{classfill}{HTML}{DDEEFF}
% \definecolor{extfill}{HTML}{E8E8E8}

  % MLIPs ontology modules.
  % Training Data shifted +2em right; ML-Schema shifted +2em right;
  % CMSO shifted +3em right.  1em is taken as 0.35cm here.
  \node[nodemod] (method) at (0, 1.25)       {Method};
  \node[nodemod] (train)  at (4.72, -0.625)  {Training\\Data};
  \node[nodemod] (bench)  at (-2.17, -0.625) {Benchmark};

  % External ontologies
  \node[nodeext] (mls)    at (3.45, 1.6)   {MLS};
  \node[nodeext] (prov)   at (0, 3.35)     {PROV-O};
  \node[nodeext] (mdo)    at (5.75, 2.25)  {MDO};
  \node[nodeext] (cmso)   at (7.3, -0.6)   {CMSO};
  \node[nodeext] (schema) at (-5, -0.6)    {schema.org};
  \node[nodeext] (qudt)   at (1, -0.5)     {QUDT};

  % Method -- ML-Schema: six axioms collapsed onto one arc
  \draw[arc, line width=3.0pt] (method) to
    node[edgelabel, pos=0.5, align=center] {L1, L2 \\ L3, L11 \\ L12, L13} (mls);

  % Method -- other external ontologies
  \draw[arc, line width=0.5pt] (method) to node[edgelabel, pos=0.5] {L5} (prov);
  \draw[arc, line width=0.5pt, bend right, looseness=1.25] (method) to
    node[edgelabel, pos=0.5] {L15} (qudt);

  % Training Data -- external
  \draw[arc, line width=0.5pt] (train) to node[edgelabel, pos=0.5] {L4} (mls);
  \draw[arc, line width=0.5pt, bend right=45, looseness=1.5] (train) to
    node[edgelabel, pos=0.5] {L6} (prov);
  \draw[arc, line width=0.5pt, bend right=15] (train) to
    node[edgelabel, pos=0.5] {L8} (mdo);
  \draw[arc, line width=1.0pt] (train) to
    node[edgelabel, pos=0.5, align=center] {L9 \\ L10} (cmso);

  % Benchmark -- external
  \draw[arc, line width=0.5pt, bend left=60, looseness=1.25] (bench) to
    node[edgelabel, pos=0.5] {L7} (prov);
  \draw[arc, line width=0.5pt] (bench) to node[edgelabel, pos=0.5] {L14} (schema);

  % Internal cross-module arcs
  \draw[arc, line width=0.5pt] (method) to node[edgelabel, pos=0.5] {L16} (train);
  \draw[arc, line width=0.5pt] (bench)  to node[edgelabel, pos=0.5] {L17} (method);
  \draw[arc, line width=0.5pt, bend right] (bench)  to node[edgelabel, pos=0.5] {L18} (train);

\end{tikzpicture}
