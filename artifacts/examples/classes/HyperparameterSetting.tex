A concrete setting of the cutoff radius to 5.0~\AA{} for a specific training configuration.
\begin{lstlisting}[language=turtle]
ex:setting-cutoff a mlips:HyperparameterSetting ;
    mlips:forHyperparameter ex:hp-cutoff-radius ;
    mlips:settingValue "5.0"^^xsd:float ;
    mlips:cutoffRadius 5.0 ;
    qudt:hasUnit unit:ANGSTROM .
\end{lstlisting}
