A trained C14-MTP for TiCr$_2$-H, linking back to method and training data.
\begin{lstlisting}[language=turtle]
ex:model-mtp-TiCr2H a mlips:TrainedModel ;
    rdfs:label "C14-MTP for TiCr2-H" ;
    mlips:trainedWith ex:MTP ;
    mlips:trainedOn ex:ds-TiCr2H .
\end{lstlisting}
