A trained MACE model for the Ti-Al system, linking back to algorithm and training data.
\begin{lstlisting}[language=turtle]
ex:mace-tial-model a mlips:TrainedModel ;
    rdfs:label "MACE Ti-Al model" ;
    mlips:trainedWith ex:MACE ;
    mlips:trainedOn ex:tial-dataset .
\end{lstlisting}
