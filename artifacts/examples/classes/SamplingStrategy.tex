An ad-hoc \texttt{ChemicalSampling} individual declared inline.
\begin{lstlisting}[language=turtle]
ex:ChemicalSampling a mlips:SamplingStrategy ;
    rdfs:label "Chemical sampling" ;
    rdfs:comment "Configurations sampled across compositions." .
\end{lstlisting}
