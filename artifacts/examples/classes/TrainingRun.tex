A training run that executes the MACE algorithm on a Ti-Al dataset and produces a trained model.
\begin{lstlisting}[language=turtle]
ex:run-mace-tial a mlips:TrainingRun ;
    mlips:executes ex:MACE ;
    mlips:runsOn ex:tial-dataset ;
    mlips:produces ex:mace-tial-model .
\end{lstlisting}
