A paper-local candidate-vocabulary instance for a Gaussian basis set
that is not (yet) in the controlled vocabulary, plus the
\texttt{candidateForVocabulary} flag that signals it for promotion review.
\begin{lstlisting}[language=turtle]
ex:basis-6-31g-d-smith2017 a mlips:DftBasisSet ;
    rdfs:label "6-31G(d)" ;
    rdfs:comment "Pople-style split-valence double-zeta Gaussian basis with d polarisation." ;
    mlips:candidateForVocabulary mlips:DftBasisSet .
\end{lstlisting}
