Reference settings are abstracted across method families; concrete subclasses carry the specific parameters.
\begin{lstlisting}[language=turtle]
ex:ccsdt-water mlips:hasReferenceSettings ex:ccsdt-settings .
ex:ccsdt-settings a mlips:WaveFunctionSettings ;  # subclass of ReferenceSettings
    mlips:usedReferenceCode ex:lib-pyscf .
\end{lstlisting}
