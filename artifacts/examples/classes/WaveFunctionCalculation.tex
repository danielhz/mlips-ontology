A CCSD(T) calculation used to generate reference data for an MLIP study.
\begin{lstlisting}[language=turtle]
ex:ccsdt-water a mlips:WaveFunctionCalculation ;
    mlips:hasWaveFunctionSettings ex:ccsdt-settings .
\end{lstlisting}
