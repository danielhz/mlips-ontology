An MLIP run that applies the MTP method on a TiCr$_2$-H dataset and produces a trained model.
\begin{lstlisting}[language=turtle]
ex:run-mtp-TiCr2H a mlips:MLIPRun ;
    mlips:appliesMethod ex:MTP ;
    mlips:runsOn ex:ds-TiCr2H ;
    mlips:produces ex:model-mtp-TiCr2H .
\end{lstlisting}
