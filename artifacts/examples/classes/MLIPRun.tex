An MLIP run that applies the MACE method on a Ti-Al dataset and produces a trained model.
\begin{lstlisting}[language=turtle]
ex:run-mace-tial a mlips:MLIPRun ;
    mlips:appliesMethod ex:MACE ;
    mlips:runsOn ex:tial-dataset ;
    mlips:produces ex:mace-tial-model .
\end{lstlisting}
