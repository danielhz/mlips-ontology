The MTP functional form: a polynomial in moment-tensor contractions evaluated over the local neighborhood.
\begin{lstlisting}[language=turtle]
ex:mtp-functional-form a mlips:FunctionalForm ;
    rdfs:label "MTP moment-tensor polynomial" .
\end{lstlisting}
