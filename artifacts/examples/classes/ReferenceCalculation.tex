A reference calculation can be either a DFT calculation or a wave-function calculation; the supertype is what is required by the dataset.
\begin{lstlisting}[language=turtle]
ex:ds-water mlips:hasReferenceCalculation ex:ccsdt-water .
ex:ccsdt-water a mlips:WaveFunctionCalculation .  # subclass of ReferenceCalculation
\end{lstlisting}
