A training dataset declaring that it covers energy, forces, and stresses using the predefined individuals.
\begin{lstlisting}[language=turtle]
ex:tial-dataset mlips:coversProperty
    mlips:Energy ,
    mlips:Forces ,
    mlips:Stresses .
\end{lstlisting}
