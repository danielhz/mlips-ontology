A single atomic configuration belonging to the Ti-Al training dataset.
\begin{lstlisting}[language=turtle]
ex:config-0042 a mlips:AtomicConfiguration ;
    rdfs:label "Ti-Al config #42" .

ex:tial-dataset mlips:hasConfiguration ex:config-0042 .
\end{lstlisting}
