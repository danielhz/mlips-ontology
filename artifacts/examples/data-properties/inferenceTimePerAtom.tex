A benchmark that measured 1.5\,$\mu$s per atom inference time on the
test system.
\begin{lstlisting}[language=turtle]
ex:result-mtp-ticr2h-c15 mlips:inferenceTimePerAtom "1.5"^^xsd:double .
\end{lstlisting}
