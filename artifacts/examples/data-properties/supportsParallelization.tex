MACE supports multi-node and multi-core parallel training and
inference (e.g., via DDP or MPI).
\begin{lstlisting}[language=turtle]
ex:MACE mlips:supportsParallelization "true"^^xsd:boolean .
\end{lstlisting}
