An MLIP run may optionally be linked to an underlying ML-Schema run that records the training algorithm execution.
\begin{lstlisting}[language=turtle]
ex:run-mtp-TiAl mlips:hasTrainingRun ex:mls-run-mtp-TiAl .
ex:mls-run-mtp-TiAl a mls:Run ;
    mls:executes ex:lbfgs .
\end{lstlisting}
