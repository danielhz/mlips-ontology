A benchmark result records the hyperparameter setting used during evaluation.
\begin{lstlisting}[language=turtle]
ex:result-01 mlips:hasHyperparameterSetting
    ex:setting-cutoff-5A .
\end{lstlisting}
