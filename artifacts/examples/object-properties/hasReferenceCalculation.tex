A training dataset whose reference data was produced by a wave-function calculation. \texttt{hasDFTCalculation} is a subproperty, so a query on \texttt{hasReferenceCalculation} also matches DFT-based datasets.
\begin{lstlisting}[language=turtle]
ex:ds-water mlips:hasReferenceCalculation ex:ccsdt-water .
\end{lstlisting}
