Generic link from a reference calculation to its settings. \texttt{hasDFTSettings} is the DFT-specific subproperty.
\begin{lstlisting}[language=turtle]
ex:ccsdt-water mlips:hasReferenceSettings ex:ccsdt-settings .
\end{lstlisting}
