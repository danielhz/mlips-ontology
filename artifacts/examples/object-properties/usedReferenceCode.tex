A wave-function calculation used PySCF as the software code. The DFT-specific \texttt{usedDFTCode} is a subproperty.
\begin{lstlisting}[language=turtle]
ex:ccsdt-settings mlips:usedReferenceCode ex:lib-pyscf .
\end{lstlisting}
