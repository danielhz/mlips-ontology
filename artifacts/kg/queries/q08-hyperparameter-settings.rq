# Q8 -- Hyperparameter settings.
# All triples whose subject is a HyperparameterSetting.

PREFIX mlips: <https://w3id.org/mlips#>

CONSTRUCT { ?s ?p ?o }
WHERE {
  ?s a mlips:HyperparameterSetting .
  ?s ?p ?o .
}
